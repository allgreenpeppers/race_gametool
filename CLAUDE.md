# CLAUDE.md

@AGENTS.md

The file above is this repo's canonical, tool-agnostic engineering-quality
guide (layer isolation invariant, dart:ui isolation, the verification loop,
testing conventions, style). It applies in full to Claude Code sessions.

Nothing Claude-Code-specific beyond that yet — if you add tooling that only
Claude Code uses (a slash command, a hook), document it here rather than in
`AGENTS.md`, which other agents also read.
